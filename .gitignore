node_modules/
node_modules
.env
server.log
server.err
.claude/settings.local.json

# Dossiers de travail / itérations design — non déployés sur le site live
_dev/
_mockup/
# _analyse-complete/ et _analyse-visuelle/ sont POUSSÉS sur GitHub
# pour sauvegarde des audits, mais bloqués dans robots.txt (pas indexés par Google)

# Fichiers de backup historiques (archives, ne pas indexer)
*-backup-*.html
*-old-*.html
index-original.html
js/pliage-backup-avant-patches.js

# Images originales (PNG/JPG) remplacées par WebP — backups gardés en local
assets/photos/_orig/
assets/photos/*.png
assets/photos/*.jpg
assets/photos/*.jpeg
